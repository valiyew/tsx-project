import React, { Component } from 'react'
import arrays from './arrays'

export default class Num extends Component {
  render() {
    return (
      <div>
        <span>{arrays.map((arrays: any) => (
        
        ))}</span>
      </div>
    )
  }
}
